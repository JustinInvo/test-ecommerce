// Tailwind removed per project brief: pure CSS Modules + CSS variables only.
// Keeping an empty plugin map satisfies any downstream tool that expects this file.
const config = { plugins: {} };
export default config;
